# تغییرات نسخه ۱.۲ — رفع دو باگ اصلی

## ۱) تایمر اشتباه می‌شمرد
**چه بود:** ده دقیقه استفاده یک دقیقه حساب می‌شد و هر باز/بسته کردن بازی یک دقیقه‌ی الکی اضافه می‌کرد.

**علت:** مصرف از روی «جفت‌کردن رویدادهای UsageStats» بازسازی می‌شد
(`MOVE_TO_FOREGROUND` با `MOVE_TO_BACKGROUND`). این روش روی گوشی‌های واقعی می‌لنگد:
جلسه‌های باز گم می‌شوند، رویدادهای هر اکتیویتی جدا شمرده می‌شوند و سیستم بعضی رویدادها را
گرد یا خلاصه می‌کند. نتیجه: زمان طولانی گم می‌شد و هر جابه‌جایی یک تکه‌ی اشتباه اضافه می‌کرد.

**چه شد:** شمارش کامل بازنویسی شد.
- `usage/UsageLedger.kt` (جدید): مصرف امروز هر برنامه را خودمان نگه می‌داریم؛ در حافظه جمع می‌شود
  و هر ۱۰ ثانیه روی دیسک ذخیره می‌شود تا با کشته شدن پروسه از دست نرود. با تغییر روز خودکار صفر می‌شود.
- `usage/ForegroundWatcher.kt` (جدید): از رویدادهای سیستم **فقط برای شناسایی** برنامه‌ی جلو استفاده
  می‌کند (که قابل اتکاست)، با یک نشانگر تا هر ثانیه کل روز دوباره خوانده نشود.
- `service/MonitorService.kt`: تیک از ۲ ثانیه به **۱ ثانیه** آمد و هر تیک زمان **واقعی سپری‌شده** را
  به مصرف برنامه‌ی جلو اضافه می‌کند. اندازه‌گیری با ساعت خودمان، نه با حدس زدن از رویدادها.
- سقف امن ۳ ثانیه برای هر تیک: اگر سرویس چند دقیقه کشته شده باشد، آن فاصله اشتباهی اضافه نمی‌شود.
- وقتی **صفحه خاموش یا قفل** است زمان شمرده نمی‌شود (قبلاً گوشی در جیب هم زمان می‌سوزاند).
- عدد مصرف در لیست هر ثانیه زنده به‌روز می‌شود، پس با چشم می‌بینی تایمر درست کار می‌کند.
- زیر ۱ دقیقه هم حالا «ثانیه» نشان داده می‌شود (قبلاً همه‌چیز به دقیقه گرد می‌شد و «۰ دقیقه» می‌دیدی).
- دکمه‌ی **«صفر کردن مصرف امروز»** به پاپ‌آپ اضافه شد تا تست کردن راحت باشد.

## ۲) هیچ تبلیغی نمایش داده نمی‌شد
**علت اصلی:** تبلیغ آنی ۹۰۰ میلی‌ثانیه بعد از ورود «نمایش» داده می‌شد، در حالی که درخواست هنوز از
سرور برنگشته بود. کد می‌دید تبلیغی آماده نیست، بی‌صدا رد می‌شد و **دیگر هیچ‌وقت دوباره تلاش نمی‌کرد**.
بنر هم فقط یک بار در لحظه‌ی ساخت صفحه درخواست می‌فرستاد؛ چون SDK با ContentProvider تازه بالا می‌آمد،
آن یک درخواست خطا می‌داد و بنر برای همیشه خالی می‌ماند.

**چه شد:**
- **صف نمایش:** با ورود کاربر، نمایش «در صف» می‌رود. لحظه‌ای که تبلیغ از سرور رسید، اگر صفحه هنوز
  باز باشد فوراً نشان داده می‌شود (پنجره‌ی ۲۰ ثانیه‌ای، بعد از آن مزاحم کاربر نمی‌شود).
- **تلاش مجدد:** درخواست ناموفق تا ۴ بار با فاصله‌ی ۵ ثانیه دوباره امتحان می‌شود. برای بنر ۴ بار با
  فاصله‌ی ۶ ثانیه.
- **صبر برای آماده شدن SDK:** با `Tapsell.setInitializationListener` به‌محض بالا آمدن SDK درخواست
  تازه فرستاده می‌شود.
- **پاس دادن Activity** در درخواست تبلیغ (بعضی شبکه‌ها مثل اپلوین بدون آن تبلیغ نمی‌دهند).
- بنر ارتفاع ثابت ۵۲dp گرفت تا `wrap_content` صفر نشود و جای بنر واقعاً وجود داشته باشد.
- **کارت وضعیت تبلیغات** (فقط در بیلد دیباگ): مرحله‌ی فعلی را نشان می‌دهد و دو دکمه دارد،
  «دریافت مجدد» و «نمایش تبلیغ». اگر باز هم تبلیغی نیامد، متن خطای خود تپسل را همان‌جا می‌بینی.
- همه‌ی لاگ‌ها با تگ `TapsellAds` و `TapsellBanner` در Logcat هستند.

## اگر باز هم تبلیغ نیامد
کارت دیباگ متن دقیق خطا را می‌گوید. حالت‌های رایج:
- **«تبلیغی موجود نبود»**: زون تازه ساخته شده و هنوز موجودی نگرفته، یا نوع زون در پنل با نوع درخواست
  نمی‌خورد. زون `6aa5ca2b1f07c00619f45129` باید در پنل **Interstitial** باشد و
  `6aa5cae6796a202335abbc77` باید **Standard Banner** باشد.
- **خطای احراز هویت / app key**: یعنی App ID به SDK نرسیده. `TAPSELL_APP_ID` در `gradle.properties`
  را چک کن و یک بار `Build > Clean Project` بزن (manifest placeholder کش می‌شود).
- بنر ۳۲۰×۵۰ اگر پر نشد، در `TapsellBanner.kt` مقدار `BannerSize.BANNER_320_50` را به
  `BannerSize.BANNER_ADAPTIVE` عوض کن.
